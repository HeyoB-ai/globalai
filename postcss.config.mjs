// Bewust leeg: Tailwind CSS v4 wordt door de Vite-plugin (@tailwindcss/vite)
// verwerkt, niet via PostCSS. Dit lokale bestand voorkomt dat Vite omhoog in de
// mappenboom een andere (v3) PostCSS-config oppikt.
export default {
  plugins: {},
};
